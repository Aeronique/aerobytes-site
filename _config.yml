title: aerobytes
description: security research • CTF writeups • threat intelligence
url: "https://aerobytes.io"
baseurl: ""

# Build settings
markdown: kramdown
permalink: /writeups/:title/

# Collections
collections:
  posts:
    output: true
    permalink: /writeups/:title/

# Exclude from build
exclude:
  - README.md
  - LICENSE.txt
  - DEPLOYMENT.md

# Force-publish dot folders Jekyll skips by default (serves /.well-known/security.txt)
include:
  - .well-known

# Plugins
plugins:
  - jekyll-feed
  - jekyll-seo-tag
